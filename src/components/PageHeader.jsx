const PageHeader = ({ pageTitle }) => {
  return (
    <div className="page-header text-center">
      <h2 className="text-white fw-bolder">{pageTitle}</h2>
    </div>
  );
};

export default PageHeader;
